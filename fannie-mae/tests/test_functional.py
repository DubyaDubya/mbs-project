import pytest

def test_full():
    pytest.fail("complete the test")
    #script takes a csv name and folder, as well as a parquet folder
    #generates an appropriate parquet name from csv name
    
    #script reads csv and gets all columnnames with datatypes

    #script writes csv to parquet at correct location
    
    #items selected at random index from csv are the same as items
    #at the same index in parquet